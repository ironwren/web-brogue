 type Query {
    games(username: String!): [Game!]
}

type Game {
    id: ID!
    username: String!
    score: Int!,
    seed: Int!,
    level: Int!,
    result: String!,
    easyMode: Boolean!,
    description: String!,
    date: String!,
    variant: String!
}